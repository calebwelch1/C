// when working with ints they cannot hold decimals
// when dividing integers wont hold decimal portion
// so must change at least one to float in order to return decimal

int x = 2;
float y = 3;
float z = 0;
z = x / y;